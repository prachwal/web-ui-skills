# Session and storage

- Keep sensitive session material out of `localStorage` when possible.
- Prefer secure cookies for browser sessions when the architecture supports it.
- Treat query strings, logs, and analytics payloads as potentially exposed.
