# Playwright basics

- Playwright Test supports Chromium, Firefox, and WebKit.
- Use the HTML or JSON reporter to inspect failures.
- Use codegen for quick test scaffolding when helpful.
- Run tests headless in CI and headed only when debugging.
